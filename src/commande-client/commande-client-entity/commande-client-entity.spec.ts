import { CommandeClientEntity } from './commande-client-entity';

describe('CommandeClientEntity', () => {
  it('should be defined', () => {
    expect(new CommandeClientEntity()).toBeDefined();
  });
});
