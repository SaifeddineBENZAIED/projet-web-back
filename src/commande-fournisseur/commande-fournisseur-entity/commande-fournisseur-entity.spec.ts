import { CommandeFournisseurEntity } from './commande-fournisseur-entity';

describe('CommandeFournisseurEntity', () => {
  it('should be defined', () => {
    expect(new CommandeFournisseurEntity()).toBeDefined();
  });
});
